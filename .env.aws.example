# AWS Production Environment Variables
#
# For running Docker Compose on AWS EC2
#
# Usage:
#   1. Copy this file: cp .env.aws.example .env
#   2. Update the values below with your actual configuration
#   3. Run: docker-compose up -d

# ============================================
# Database Configuration (RDS in VPC)
# ============================================
# RDS endpoint (accessible directly within VPC, no SSH tunnel needed)
MYSQL_HOST=your-rds-endpoint.rds.amazonaws.com
MYSQL_USER=your_database_user
MYSQL_PASSWORD=your_database_password
MYSQL_DATABASE=fullbay

# ============================================
# S3 Configuration
# ============================================
# EC2 instance should have an attached IAM Role with the following S3 permissions:
#   - s3:GetObject
#   - s3:PutObject
#   - s3:ListBucket
#
# No need to set AWS_ACCESS_KEY_ID and AWS_SECRET_ACCESS_KEY
# Docker containers will automatically retrieve temporary credentials from EC2 instance metadata

S3_BUCKET=fullbay-migration
S3_PREFIX=fullbay-output
AWS_REGION=us-east-1

# ============================================
# Application Configuration (Optional)
# ============================================
# PRETTY_JSON=false
